# student-database-importer
Automated student database importer built with Bash and PostgreSQL. Imports CSV data into a normalized relational database while maintaining data integrity and relationships.
